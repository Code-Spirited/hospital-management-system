# Base URL for the real backend API, once one exists. Vite only exposes
# variables prefixed VITE_ to client-side code (import.meta.env.VITE_*).
# Falls back to http://localhost:5000/api if unset — see src/services/apiClient.js.
VITE_API_BASE_URL=http://localhost:5000/api